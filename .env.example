# Optional: enables live AI explanations and codebase chat.
OPENAI_API_KEY=

# Optional: model used when OPENAI_API_KEY is configured.
OPENAI_MODEL=gpt-5-mini

# Optional: validates GitHub webhook payloads when configured.
GITHUB_WEBHOOK_SECRET=

# Optional locally. Render sets PORT automatically in production.
PORT=3000
